{
  "name": "frontend",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate && node scripts/generate-sitemap.js",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npm run generate && firebase deploy --only hosting",
    "sitemap": "node scripts/generate-sitemap.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@nuxt/image": "^2.0.0",
    "@nuxtjs/i18n": "^10.2.1",
    "@nuxtjs/sitemap": "^7.5.0",
    "@nuxtjs/tailwindcss": "^6.14.0",
    "firebase": "^12.6.0",
    "nuxt": "^4.2.1",
    "vue": "^3.5.24",
    "vue-router": "^4.6.3"
  }
}
